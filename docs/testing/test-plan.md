# FraudGuard — Test Plan

**Version:** 1.0 · **Date:** 2026-09-12 (Phase 8)

Companion to [test-strategy.md](./test-strategy.md) (the why/approach per test type) — this
is the concrete "what exists, what it tests, why, and what failure it would catch" per test
case, for the tiers Phase 8 actually delivers: resilience, the producer↔consumer contract,
and E2E. Unit tests (300+, `packages/*/src/**/*.test.ts`) and the existing integration
suite carry their own what/why/catches in each test file's own doc comments — re-listing
every one here would drift from the source of truth the moment either changed; this
document indexes them by file instead of duplicating their content.

---

## 1. Resilience — every row of ADR-005's policy table

| Dependency           | Row's policy                              | Test file                                                                                                              | What it proves                                                                                                                                                                                                                                                                                                                                                 | What failure it would catch                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------- | ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Redis                | Cautious-open                             | `tests/resilience/redis-outage.resilience.test.ts`                                                                     | The SAME transaction ALLOWs with Redis healthy, lands in REVIEW once Redis is down (the widened band, not just the `degraded` flag), and ALLOWs again on recovery                                                                                                                                                                                              | The degraded policy existing in config/docs but the actual threshold-widening never firing; a fallback that doesn't recover cleanly                                                                                                                                                                                                                                                                                                                                                      |
| ML provider          | Fallback                                  | —                                                                                                                      | **Deferred to Phase 10.** `MLScoringProvider` does not exist yet (RISK-003) — building a circuit breaker around a provider that has no real failure mode to breaker-trip would be testing a mock, which this project's own rules forbid writing prematurely (CON-005's whole point: ML is a controlled comparison against a working baseline, not built early) | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Kafka                | Invisible                                 | `tests/resilience/kafka-outage.resilience.test.ts`                                                                     | A real stopped/restarted `kafka` container: authorizations unaffected throughout, outbox drains fully on recovery                                                                                                                                                                                                                                              | Any accidental hot-path dependency on Kafka reachability (ADR-001)                                                                                                                                                                                                                                                                                                                                                                                                                       |
| PostgreSQL           | Fail closed                               | `tests/resilience/postgres-outage.resilience.test.ts`                                                                  | A real stopped/restarted `postgres` container: `503` + `Retry-After`, never a silent `200`, never a crash, normal service resumes after recovery                                                                                                                                                                                                               | **Found live, writing this test:** no code path actually returned `503` at all (an unhandled connectivity error fell through to a generic `500`); `packages/persistence`'s `Pool`s had no `'error'` listener, so an idle connection erroring during the outage would have crashed the whole process outright                                                                                                                                                                             |
| `fraud-api` instance | Transparent                               | —                                                                                                                      | **Deferred.** "Traffic simply moves" requires a gateway/load balancer in front of `fraud-api`, which Phase 3 deliberately did not build (ARCHITECTURE.md §14 question 3, still open) — there is nothing to fail over TO yet. Revisit once a gateway exists                                                                                                     |
| `event-worker`       | Deferred (lag grows, hot path unaffected) | `tests/resilience/kafka-outage.resilience.test.ts` + `tests/integration/event-worker.integration.test.ts`'s IT-EVT-001 | From `fraud-api`'s perspective, "Kafka is down" and "nothing is consuming from Kafka" are the identical observable effect — outbox rows accumulate, the hot path is unaffected either way. IT-EVT-001 separately proves the relay's own catch-up (a backlog drains fully once it runs), which is the "resumes from committed offsets" half of this row's claim | Cross-referenced rather than duplicated — a dedicated `event-worker` process kill/restart test would prove the identical mechanism these two already do                                                                                                                                                                                                                                                                                                                                  |
| Overload             | Shed                                      | `tests/resilience/overload-shedding.resilience.test.ts`                                                                | 20x the concurrency ceiling fired at once, over REAL sockets: some requests shed with `429`+`Retry-After`, the rest served normally                                                                                                                                                                                                                            | **Found live, writing this test:** `MAX_CONCURRENT_REQUESTS` existed as config since Phase 1 with nothing ever reading it — `@fastify/rate-limit` enforces a requests-per-window limit, a different mechanism entirely. Also found: `.inject()` (light-my-request) does not exercise genuine request overlap at all — every attempt measured zero shedding regardless of ceiling, until the test was rewritten to `app.listen()` on a real port and fire real concurrent `fetch()` calls |

**Additional resilience rows beyond ADR-005's table itself** (named in
`docs/testing/test-strategy.md`'s own §3.6 table, not literal ADR-005 rows):

| Test                                                 | Test file                                                             | What it proves                                                                                                                                                                                                          | What failure it would catch                                                                                      |
| ---------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| `RT-DUP-001` Duplicate delivery                      | `tests/integration/event-worker.integration.test.ts` (IT-EVT-002/003) | The same published event fed to a consumer twice produces one audit row / one case, never two                                                                                                                           | A consumer that isn't actually idempotent despite at-least-once delivery (ADR-006)                               |
| `RT-SLOW-001` Dependency latency (not outright down) | `tests/resilience/dependency-latency.resilience.test.ts`              | Redis paused (`CLIENT PAUSE`, not stopped) longer than `REDIS_TIMEOUT_MS`: the app times out fast, falls back, and the WHOLE request completes in a small multiple of the pause — no retry storm, no cascade            | `maxRetriesPerRequest` silently not being honoured; a timeout that doesn't actually trigger the ADR-005 fallback |
| `RT-MALFORM-001` Malformed message                   | `tests/resilience/malformed-message.resilience.test.ts`               | A genuinely non-JSON message published directly to a real topic: routed to `<topic>.dlq` with no retry, AND a valid message published right after on the same topic is still processed — the consumer did not get stuck | A consumer that crashes, retries forever, or blocks its partition on one malformed message                       |
| `RT-BULK-001` Hot/cold bulkhead (NFR-007)            | `tests/resilience/bulkhead-isolation.resilience.test.ts`              | `coldPool` saturated with 20 concurrent `pg_sleep(0.5)` queries (4x `POSTGRES_POOL_COLD_MAX`): authorization latency stays in the same tens-of-milliseconds range as the unsaturated baseline                           | Hot and cold paths accidentally sharing one connection pool, or any other bulkhead leak                          |
| `RT-INST-001` Instance loss                          | —                                                                     | **Deferred**, same reason as the ADR-005 "fraud-api instance" row above — no gateway exists to demonstrate failover through                                                                                             |
| `RT-ML-001` ML provider resilience                   | —                                                                     | **Deferred to Phase 10**, same reason as the ADR-005 "ML provider" row above                                                                                                                                            |

**Every resilience test file stops/restarts a real container, fires real concurrent
connections, or injects a real protocol-level fault** — never a mocked failure. Verified
stable across repeated consecutive runs, individually and as the full `pnpm
test:resilience` suite together, before being called done (the same bar every phase in
this project has applied).

---

## 2. Contract — producer ↔ consumer

| Test file                                          | What it proves                                                                                                                                                                                                                                 | What failure it would catch                                                                                                                                                                                                                                             |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/contract/event-schema-contract.test.ts`     | Every event type's producer-built envelope (built the exact way the real producer builds it) round-trips through `JSON.stringify`/`JSON.parse` (the real Kafka wire shape) and parses successfully the exact way every real consumer parses it | A producer and consumer silently drifting — one side changes a payload shape, the other's parse call starts failing in production, not in a test                                                                                                                        |
| — (same file) REGRESSION GUARD                     | `schema.shape.payload.parse(<full envelope>)` is asserted to FAIL                                                                                                                                                                              | The EXACT regression Phase 6 caught live: two consumers parsed only the inner payload schema against the full envelope, and every field came back "Required". Encoded as a standing assertion, not just a historical doc comment, so it cannot be silently reintroduced |
| `tests/contract/metrics-contract.test.ts`          | Every Phase 7 metric is registered by name and type                                                                                                                                                                                            | A metric silently dropped from the registry                                                                                                                                                                                                                             |
| `tests/contract/scoring-provider.contract.test.ts` | Every `FraudScoringProvider` implementation satisfies the interface identically (CON-005)                                                                                                                                                      | A provider that type-checks but behaves incompatibly, breaking Phase 10's "ML is a configuration change" claim                                                                                                                                                          |

---

## 3. End-to-end — the journeys that must never break

| Test                                | Test file                              | What it proves                                                                                                                                                                                                                                     | What failure it would catch                                                                                                                                                                                         |
| ----------------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `E2E-001` Full REVIEW lifecycle     | `tests/e2e/full-lifecycle.e2e.test.ts` | Scored REVIEW → case created (event-worker) → listed and approved (review-api, real HTTP) → all FOUR actions (received, decided, case created, case reviewed) land in the durable audit trail — the full chain, not pieces of it proven separately | FR-008's "full basis" claim being true for pieces of the REVIEW path but not the whole of it; a review action that doesn't actually close the loop back through the outbox/audit system                             |
| `E2E-002` Degraded path, end to end | `tests/e2e/full-lifecycle.e2e.test.ts` | A Redis-degraded decision's `degraded`/`degradedReason` survive all the way into the durable `audit_events` record, not just the HTTP response                                                                                                     | ADR-005's visibility requirement #2 ("the flag and reason are persisted... so historical analysis can separate degraded from healthy decisions") being true for the response but silently false for the audit trail |

Deliberately two tests, not many — test-strategy.md §3.5's own rule: E2E is the fewest,
slowest tier, kept to the journeys that must never break, each proving something no
existing integration test already chains together in one continuous run (see the doc
comment at the top of the test file itself for exactly which gaps each one closes).

---

## 4. Where everything else lives

- **Unit** (300+ tests): `packages/*/src/**/*.test.ts`. Each file's own doc comments state
  what/why/catches per test — `packages/domain` is the pyramid's base (zero I/O, exhaustive
  coverage of rule boundaries, score combination, decision thresholds, lifecycle
  transitions, as `docs/testing/test-strategy.md` §3.1 describes).
- **Integration** (≈40 tests): `tests/integration/*.integration.test.ts`, one file per
  feature area (scoring, feature-store, event-worker, review-api, observability, demo
  scenarios, policy admin). Each carries its own what/why/catches per test.
- **Architecture**: `tests/architecture/hot-path.test.ts` — ADR-003's import-graph
  enforcement, unchanged since Phase 3.
- **Security**: Phase 11's, per `docs/security/threat-model.md` §6.
- **Load**: Phase 9's, per `docs/testing/test-strategy.md` §3.8; results land in
  [test-results/](./test-results/).

---

## 5. Honest gaps, not hidden ones

Two ADR-005 rows (ML provider, `fraud-api` instance loss) and their corresponding
`RT-ML-001`/`RT-INST-001` test strategy entries are **not** tested in Phase 8, for reasons
stated in §1 above: the infrastructure each would need to exercise for real (a working
`MLScoringProvider`, a gateway/load balancer) does not exist yet, and building either one
solely to satisfy this phase's checklist would mean testing against a prop rather than the
real thing — exactly what this project's own rules forbid (Brief §19, §45; CON-005;
RISK-003). Both rows are revisited the moment the system they describe actually exists
(Phase 10, and whenever a gateway is built respectively) — tracked here, and in
`docs/requirements/traceability-matrix.md`, as `PLANNED`, not quietly dropped.
