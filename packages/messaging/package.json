{
  "name": "@fraudguard/messaging",
  "version": "0.1.0",
  "private": true,
  "description": "Kafka producer/consumer wrappers (ADR-001, ADR-006) — cold path only. No workspace package outside apps/event-worker imports this.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b"
  },
  "dependencies": {
    "@fraudguard/config": "workspace:*",
    "kafkajs": "^2.2.4",
    "pino": "^9.5.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
